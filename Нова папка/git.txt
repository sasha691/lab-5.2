git init .              початок

git commit -m "name"   коміт

git add .приєднати

git status статус

git log --oneline  інфа

git checkout (id) переключитись id/гілки

git checkout (master)

git branch (name) створити гілку

git merge (name)  обєднати гілки по даті створення

git reset --hard HEAT~n видалити коміт

git branch -D (name) видалити гілку

git rebase master вирівнювання комітів перед merge

git remote add origin (SSH) приєднатися до github

email: komp9349@gmail.com

name: sasha691

git config --global user.email

ssh-keygen

git push u origin master

Django:

django-admin startproject PROJECT-NAME

python manage.py runserver

python manage.py startapp name

python manage.py createsuperuser



MySQL:

password QJL5&S$d**zm#>